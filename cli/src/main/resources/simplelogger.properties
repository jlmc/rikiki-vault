# Configures the slf4j-simple binding (see cli/pom.xml). Two layers: third-party libraries
# (JGit/mina-sshd) stay quiet by default (defaultLogLevel=warn), while the app's own code
# (io.github.jlmc packages, since Milestone 26) defaults to info. To see everything (useful when
# troubleshooting a real push/pull/clone against a remote), run with:
#   -Dorg.slf4j.simpleLogger.defaultLogLevel=debug
# or override just the noisy packages, e.g.:
#   -Dorg.slf4j.simpleLogger.log.org.apache.sshd=debug -Dorg.slf4j.simpleLogger.log.org.eclipse.jgit=debug
# scripts/run.sh wraps the app's own log.io.github.jlmc level behind a simple --log-level flag.

org.slf4j.simpleLogger.defaultLogLevel=warn
org.slf4j.simpleLogger.log.io.github.jlmc=info
org.slf4j.simpleLogger.showDateTime=true
org.slf4j.simpleLogger.dateTimeFormat=HH:mm:ss.SSS
org.slf4j.simpleLogger.showThreadName=false
org.slf4j.simpleLogger.showLogName=true
org.slf4j.simpleLogger.levelInBrackets=true
